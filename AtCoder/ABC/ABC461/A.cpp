#include <bits/stdc++.h>

void solve() {
	int a, d;
	std::cin >> a >> d;
	
	std::cout << (a <= d ? "Yes" : "No") << '\n';
}

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);
	
	solve();
	
	return 0;
}