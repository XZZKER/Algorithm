# 最小生成树

最小生成树一定是最小瓶颈树

## 模板

### Kruskal

贪心+并查集

``````c++
std::pair<int, bool> kruskal(int n, std::vector<std::array<int, 3>> &edges) {
	int size = n;
	std::vector<int> pa(n);
	std::iota(pa.begin(), pa.end(), 0);
	
	auto find = [&](this auto &&find, int i) -> int {
		return pa[i] == i ? i : (pa[i] = find(pa[i]));
	};
	
	auto merge = [&](int i, int j) -> void {
		int x = find(i), y = find(j);
		if (x == y)
			return;
		pa[x] = y;
		size--;
	};
	
	std::sort(edges.begin(), edges.end(), [](auto &x, auto &y) -> bool {
		return x[2] < y[2];
	});
	
	int weight = 0;
	for (auto &edge : edges) {
		if (find(edge[0]) != find(edge[1])) {
			weight += edge[2];
			merge(edge[0], edge[1]);
		}
	}
	
	return {weight, size == 1};
}
``````

## 题目

### 基础

1. [模板](https://www.luogu.com.cn/problem/P3366)
2. [建图+贪心](https://www.luogu.com.cn/problem/P1194)
3. [贪心](https://leetcode.cn/problems/checking-existence-of-edge-length-limited-paths)
4. [最小瓶颈树](https://www.luogu.com.cn/problem/P2330)